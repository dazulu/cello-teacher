[build]
  command = "npm run build:lambda && npm run build && npm run export"
  publish = "out"
  functions = "lambda"
[[headers]]
  for = "*.avif"
  [headers.values]
    Content-Type = "image/avif"
    Content-Disposition = "inline"